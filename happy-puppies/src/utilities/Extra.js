// Button Code For Upload

{
	/* <button
					type="button"
					onClick={() => {
						document.getElementById("image").click();
					}}
					className="rounded-full bg-blue-500 px-8 py-2 font-bold text-white">
					Upload Image
					<input
						id="image"
						name="image"
						type="file"
						accept="image/*"
						onChange={adoptionFormik.handleChange}
						className="hidden"
					/>
				</button> */
}

// Image validation

// image: Yup.mixed().test(
// 	"required",
// 	"Please select breed for photo",
// 	(value) => {
// 		return value != null;
// 	}
// ),
// .test("type", "Only supports .jpg/.jpeg/.png", function (value) {
// 	return (
// 		value &&
// 		(value.split(".").pop() === "jpg" ||
// 			value.split(".").pop() === "jpeg" ||
// 			value.split(".").pop() === "png")
// 	);
// }),

// Image source hardcoded
// {require(`./../images/dog-images/${item.image}.jpg`)}
